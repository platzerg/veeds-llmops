# Test with function format
- description: "[tn-001] Valid entry"
  vars:
    yaml_entry: |
      materialNumber: ABC-12345
      description: Bremsscheibe vorne links
      unit: mm
      valueRange:
        min: 20
        max: 35
      category: Bremsanlage
  assert:
    - type: javascript
      value: |
        (function() {
          const p = JSON.parse(output);
          return p.isValid === true;
        })();