- description: "Simple debug test"
  vars:
    yaml_entry: |
      materialNumber: ABC-12345
      description: Bremsscheibe vorne links
      unit: mm
      valueRange:
        min: 20
        max: 35
      category: Bremsanlage
  assert:
    - type: is-json
    - type: javascript
      value: |
        console.log("Raw output:", JSON.stringify(output));
        const parsed = JSON.parse(output);
        console.log("Parsed object:", JSON.stringify(parsed));
        console.log("isValid:", parsed.isValid);
        console.log("errors length:", parsed.errors.length);
        return true;