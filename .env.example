# =============================================================================
# VEEDS LLMOps Stack - Environment Variables
# =============================================================================
# Copy this file:  cp .env.example .env
# Generate secrets: openssl rand -hex 32
# =============================================================================

# --- Langfuse Auth Secrets (CHANGE THESE!) ---
NEXTAUTH_SECRET=REPLACE_ME_openssl_rand_hex_32
SALT=REPLACE_ME_openssl_rand_hex_16
ENCRYPTION_KEY=REPLACE_ME_openssl_rand_hex_32_must_be_64_hex_chars

# --- Langfuse URL ---
NEXTAUTH_URL=http://localhost:3000
LANGFUSE_PORT=3000

# --- PostgreSQL ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=REPLACE_ME_strong_password
POSTGRES_DB=langfuse

# --- ClickHouse ---
CLICKHOUSE_USER=clickhouse
CLICKHOUSE_PASSWORD=REPLACE_ME_strong_password

# --- Redis ---
# Leave empty for no password (local dev only)
REDIS_PASSWORD=

# --- MinIO (S3) ---
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=REPLACE_ME_strong_password
MINIO_PORT=9090
S3_BUCKET=langfuse

# --- Langfuse Headless Init (optional) ---
# Uncomment and configure to auto-create org/project/user on first startup
# LANGFUSE_INIT_ORG_ID=veeds-org
# LANGFUSE_INIT_ORG_NAME=VEEDS CORP
# LANGFUSE_INIT_PROJECT_ID=veeds-proofreader
# LANGFUSE_INIT_PROJECT_NAME=VEEDS Proofreader
# LANGFUSE_INIT_PROJECT_PUBLIC_KEY=pk-lf-veeds-dev
# LANGFUSE_INIT_PROJECT_SECRET_KEY=sk-lf-veeds-dev
# LANGFUSE_INIT_USER_EMAIL=guenter@example.com
# LANGFUSE_INIT_USER_NAME=Guenter
# LANGFUSE_INIT_USER_PASSWORD=REPLACE_ME

# --- Langfuse API Keys (set after first login in UI) ---
# Used by your application and evaluation tools
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://localhost:3000

# --- AWS Bedrock (for VEEDS Proofreader) ---
AWS_REGION=eu-central-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Or use AWS_PROFILE for local dev
# AWS_PROFILE=default

# --- Promptfoo ---
PROMPTFOO_CACHE_ENABLED=true

# --- OpenAI API (for testing/evaluation) ---
OPENAI_API_KEY=
